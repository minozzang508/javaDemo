package jungsuk;

/**
 * Created by minho on 2017-06-15.
 */

/*
    static
    - member variable : class variable - 모든 인스턴스에서 공통적으로 사용되는 변수.
    - method : 인스턴스를 생성하지 않고도 호출이 가능한 static method.
               인스턴스 멤버를 직접 사용할 수 없다.

    final
    - class : 변경될 수 없는 클래스, 상속할 수 없다.
    - method : 변결될 수 없는 메서드, Overriding을 통해 재정의할 수 없다.
    - variable : 값을 변경할 수 없는 상수. (Constructor에서만 딱 한번 초기화 가능.)

    abstract
    - class : 클래스 내에 추상 메서드가 선언되어 있음을 의미.
    - method : 선언부만 작성하고 구현부는 작성하지 않은 추상 메서드.
abstract class를 상속받아 모든 추상메서드를 구현하지 않았다면 자손 클래스도 추상 클래스로 선언해야한다.

    access modifier : encapsulation을 하기 위한 제어자.
    - class : public, (default)
    - method : public, protected, (default), private
    - member variable : public, protected, (default), private
    - local variable : X

 */
public class JS_10_Modifier {
    public static void main(String[] args) {

    }
}
